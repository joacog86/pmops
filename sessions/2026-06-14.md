## Session 2026-06-14

### [15:00] Reusable tailored-CV system + Monzo CV
- What: Built a reusable system for job-specific CVs and produced the first instance for Monzo (PM, Business Banking EU, Barcelona).
- System (`interviews/interview-bank/cv/`): `cv_engine.py` (shared render + logo embedding + LibreOffice PDF export), `cv_content_base.py` (canonical CV = source of truth), `INDEX.md` (tracker + 4-step recipe for a new CV), `tailored/<slug>/` per posting (`job-post.md`, `content.py` overrides, `generate.py`, `notes.md`, outputs).
- Monzo tuning: reoriented from the N26 KYC/lending angle toward business banking + multi-market expansion/localization + experimentation. Rewrote summary; re-angled Xepelin (multi-country banking integration) and Despegar (localized payment methods + A/B); trimmed IOL's deep AML bullet. Verified: 0 em-dashes, no new claims, 2 pages.
- Logos: small logo left of each company (Joaco's choice). Two outputs per job — designed (logos) + ATS (image-free) for Greenhouse upload. Installed LibreOffice via brew to auto-export PDF.
- Decision: refactored the old monolithic `generate_cv_docx.py` into engine + content data so each new posting is a small overrides file, not a 300-line copy. Old scripts left in place, superseded.
- Insight: Monzo applies via Greenhouse (ATS) — logos can break text parsing, hence the separate ATS version.
- Open: minor cosmetic — long company names + logo wrap the location to a second line on IOL/Despegar/Directv (acceptable). PDF export now automated for all future CVs.

### [15:20] Restructured to per-opportunity folders
- What: Moved from `cv/tailored/<slug>/` to one folder per opportunity under `interviews/<Company - Role>/` (matches the existing `N26 - Lending` pattern). Each folder = home for CV + JD (md+pdf) + interview prep (`prep/`, built as we go).
- Engine + base content stay as shared infra in `interview-bank/cv/`. Builder is now generic: `python3 build.py "<Company - Role>"`. Added `jd_to_pdf.py` (md→PDF via markdown + LibreOffice) so each pasted JD gets a PDF.
- Monzo files relocated to `interviews/Monzo - Business Banking EU/` with a README documenting the template structure. INDEX.md + memory updated.
- Decision: Joaco pastes a posting in chat; Claude creates the whole folder (JD md+pdf, tailored CV, prep) with zero manual file work from him.
- Next step: when Joaco sends the next posting, run the full flow; start Monzo interview prep when a stage gets scheduled.

### [15:45] Logo layout polish (engine-level, applies to all CVs)
- What: Iterated company-line logo placement in cv_engine.py. Final: logo sits after `Company | Location` (not before/left), uniform size (height 0.38cm, width capped 1.5cm preserving aspect). Forced fixed table layout with explicit gridCol widths [13.9, 3.6cm] and zeroed cell padding so nothing wraps and company names align flush with section headers.
- Insight (Greenhouse/ATS): logos do NOT break Greenhouse parsing (images are ignored; recruiter sees the real PDF). The actual parse risk is the table layout, identical in both versions. So upload the designed (logo) PDF to Greenhouse; the _ATS image-free version matters only for stricter parsers (Workday/Taleo). A truly ATS-pure version would need a table-free linear layout (offered, not built).
- Open: Joaco to decide whether to keep both CV versions or drop _ATS as default.

### [16:30] Content tone-down + structure polish
- What: Dropped the image-free _ATS version (logos don't break Greenhouse). Education table: wider year col, institution moved left, added countries (Ironhack España, Digital House / ITBA Argentina), no wrap. Renamed Independent role to "Product Consultant" (Joaco: "Strategic Upskilling" sounded forced). Trimmed skills: removed Adobe XD + Qlikview/QlikSense, cut Python libs 7→3, removed the whole "PM Methods" line (listing methods is weak signal + invites "prove it"). Toned down Summary (cut buzzwords) and rewrote Personal Projects to a grounded, no-name description of the investment planner (The Dugout / InversionCockpit) with an internal chat — Joaco flagged the AI-council framing as inflated.
- Decision: keep CV language plain and modest; no project names, no method laundry lists, no buzzwords. Reinforces [[feedback-cv-writing]] / [[feedback-cv-accuracy]]. Languages stays ES+EN only (Joaco won't overstate languages).
- Open: optional further tone passes per section if Joaco wants; otherwise Monzo CV is done.
